# Service configuration

service imap-login {
  inet_listener imap {
    port = 143
  }
  inet_listener imaps {
    port = 993
    ssl = yes
  }
}

service auth {
  # Auth socket for Postfix SMTP AUTH over TCP
  inet_listener {
    port = 12345
  }
}

service auth-worker {
  user = vmail
}
